main
{
    number a = 25 * 100 * 99 / 50;
    number b = 50;
    number c;
    bool k = true;

    for(1 to 10) do
    {

    }
    new Motor[d] hey;

    addMethod(a, b);

    if(not false) then {  }

}

number Method addMethod(number k, number p)
{
    number x = k + p;

    number h = x + x + x;

    number x = p;

    return x;
}
